JWT_SECRET=replace-me-with-a-long-random-string-at-least-32-chars
# Neon example (no trailing /DATABASE — use your real db name, e.g. neondb):
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require
