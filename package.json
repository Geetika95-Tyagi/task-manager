{
  "name": "etharaai-assignment",
  "private": true,
  "description": "Monorepo root for deploy: install/build/start delegate to backend (which builds the React app into backend/public).",
  "scripts": {
    "dev": "cd backend && npm run dev:all",
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "db:migrate": "cd backend && npm run migrate:deploy"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
