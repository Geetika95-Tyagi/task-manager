{
  "name": "ethara-assignment",
  "private": true,
  "description": "Monorepo: Railway deploys one service from repo root; backend serves API + frontend build from backend/public.",
  "scripts": {
    "dev": "cd backend && npm run dev:all",
    "build": "cd backend && npm run build",
    "start": "cd backend && npm start",
    "db:migrate": "cd backend && npm run migrate:deploy"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
